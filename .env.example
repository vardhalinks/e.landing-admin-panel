# DEPRECATED: This env file is no longer used.
# The Razorpay backend (main-backend) is now the single source of truth for
# environment configuration and the shared MongoDB connection.
# Please remove or ignore this file and use the `.env` (or deployment settings)
# in the main backend repository: landing eng/razorpay-backend/.env.example

# Example reference (kept for documentation only):
# MONGO_URI=YOUR_PRODUCTION_ATLAS_URI
# JWT_SECRET=replace_with_a_long_secret
# LANDING_API_KEY=replace_with_api_key_used_by_landing
# PORT=5000
RAZORPAY_KEY_ID=replace_with_razorpay_key_id
RAZORPAY_KEY_SECRET=replace_with_razorpay_key_secret
PUBLIC_BASE_URL=http://localhost:5000
SESSION_REDIRECT=https://calendly.com/linksvardha/60min
